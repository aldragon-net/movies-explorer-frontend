import './Form.css';

function Form ({ inputs, onSubmit }) {
  return (
    <form
      className="login"
      id="login-form"
      name="login-form"
      // onSubmit={handleSubmit}
    >
    </form>
  )
}

export default Form;